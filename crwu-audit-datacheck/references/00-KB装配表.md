# references/00 · KB 逻辑装配表（crwu-audit-datacheck）

> 本表 = 库内层级路径寻址键表（路径 = crwu-dws 查询到的知识库内路径；不写知识库名称 / 本地根路径 / 节点 nodeId）。
> 使用：命中本技能 → 本表路径并入本次审核下载清单 → 经 crwu-dws 按路径实时下载正文（下载时刻即最新）→
> 只读引用，出处 = 本次下载文件:行号 + exportedAt。文件零缓存：每次审核重新下载，不做任何正文缓存。
> 分工（单一事实源）：**本技能 = 能力/算法**——工具链（xlsx/.xls 解析、公式审查）与 H0 执行、C1–C6 操作步骤留在技能内；
> **知识库 = 口径**——勾稽判定口径以 RULE 条目为准，统计口径以执行契约为准；正文经 crwu-dws 实时下载获得，
> 技能与知识库互不复制正文。
> H0 与上游机制分层：crwu-audit `references/00 §6.1` 数据隔离（raw 原件默认只归编排层、下游只读
> 已剔除隐藏数据的工作版；公式/引用坐标核查可对 raw 只读可见区，为 §6.1 唯一例外）
> 解决"隐藏脏数据是否到达本技能"；本技能 H0 解决"到达后 Excel 隐藏区（sheet/行/列/折叠组）禁读禁报"——分工不同，不互相复制。
> 索引/校验工具：`kb_tool.py`（可执行路径由部署环境注入；源仓位置 = crwu-ai `tools/kb/kb_tool.py`，README 仅源仓维护者视角）。
> 知识库文件名 / 结构 / 装配范围变更 → 同步本表并重跑 `kb_tool.py index`（判定见 `crwu-audit-optimize/references/00` §2.1）。

| 需要 | 库内层级路径（寻址键；经 crwu-dws 实时下载后只读引用） |
| --- | --- |
| 勾稽口径（M-数据校对 RULE 条目） | `06-规则库/M-数据对齐-勾稽与一致性/03-模块-数据校对.md` |
| 基准要素一致性口径 | `06-规则库/M-数据对齐-勾稽与一致性/05-模块-基准要素一致性.md` |
| 统计口径 data_diff_count / 台账 | `00-总纲/执行契约/04-审核统计与台账规范.md` |
| 判读侧映射（差异→RULE/CHK 依据示例） | 各对象/方法技能（rent/realestate 等）SKILL.md，不由本技能持有 |
