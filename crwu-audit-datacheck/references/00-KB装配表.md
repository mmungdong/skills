# references/00 · KB 逻辑装配表（crwu-audit-datacheck）

> KB 根常量（CRWU_KB_ROOT）= `~/.crwu/knowledge/knowledge-base`；本表所有 `KB/…` 均为该根下相对路径。
> 分工（单一事实源）：**本技能=能力/算法**——工具链（xlsx/.xls 解析、公式审查）与 H0 执行、C1–C6 操作步骤留在技能内；
> **KB=口径**——勾稽判定口径以 RULE 条目为准，统计口径以执行契约为准。技能与 KB 互不复制正文。
> H0 与上游机制分层：crwu-audit `references/00 §6.1` 数据隔离（raw 原件只归编排层、下游只读已删隐藏数据的工作版）
> 解决"隐藏脏数据是否到达本技能"；本技能 H0 解决"到达后 Excel 隐藏区（sheet/行/列/折叠组）禁读禁报"——分工不同，不互相复制。
> 索引/校验工具：`kb_tool.py`（可执行路径由部署环境注入；源仓位置=crwu-ai `tools/kb/kb_tool.py`，README 仅源仓维护者视角）。
> KB 文件名/结构/装配范围变更 → 同步本表并重跑 `kb_tool.py index`（判定见 `crwu-audit-optimize/references/00` §2.1）。

| 需要 | 稳定指针（CRWU_KB_ROOT 相对） |
| --- | --- |
| 勾稽口径（M-数据校对 RULE 条目） | `KB/06-规则库/M-数据对齐-勾稽与一致性/03-模块-数据校对.md` |
| 基准要素一致性口径 | `KB/06-规则库/M-数据对齐-勾稽与一致性/05-模块-基准要素一致性.md` |
| 统计口径 data_diff_count / 台账 | `KB/00-总纲/执行契约/04-审核统计与台账规范.md` |
| 判读侧映射（差异→RULE/CHK 依据示例） | 各对象/方法技能（rent/realestate 等）SKILL.md，不由本技能持有 |
