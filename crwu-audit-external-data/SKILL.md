---
name: crwu-audit-external-data
description: >-
  跨方向【外部数据核验】能力型审核（public 轴，插拔）：报告涉及收益法/市场法时，按知识库
  `M-外部数据核验` 模块规程核验报告引用的外部公开数据——抽取报告引用的外部数据项、按表 B/C 决定
  数据项与口径、经同花顺 iFinD（本环境唯一数据源）取数、口径标准化后比对，输出逐项
  【符合 / 不符合 / 请说明 / 未检查】意见。
  同花顺 iFinD 的取数入口随宿主而异：WorkBuddy 用宿主内置连接器（MCP，连接器 id `ifind-mcp`）；
  DeepSeek Harness 没有该连接器，改用 `ifind-finance-data` 技能取数——两条路径取的是同一上游 iFinD
  服务，但调用方式、凭据归属与可用性探测方式不同，见本技能 `references/01-connector-access.md`。
  本技能不启用万得，只以同花顺 iFinD 单源取数；库内表 D 为取数失败与降级口径，双源复核不适用。
  所有取数以报告基准日为准，区间与窗口由基准日推导，禁止使用取数时点的滚动窗口（否则同一项目复跑结果不同、无法回归）。
  兜底声明：同花顺 iFinD 两条取数路径都不可用时按库内降级口径降级，并在交付 HTML 的
  《外部数据核验》区显式声明 "未配置 / 未认证 同花顺 iFinD，相关条目未经外部数据核验"，
  不得静默跳过或事后补写。
  只做外部数据事实核验与取数口径披露检查，不下方法适用性与参数合理性判断（归方法轴与人工）。
  由 crwu-audit 在 methods[] 命中收益法/市场法时叠加调用；禁止脱离编排据其输出下审核判断。
---

# crwu-audit-external-data（外部数据核验 · 能力型）

> **单一事实源分工**
> **知识库 = 口径**：表 A 触发范围、表 B 数据项触及、表 C 组合映射、结论档位、
> 数据留痕要求、未检查项与缺口记录、员工补录机制——全部在库内正文，本技能不复制。
> 库内表 D（取数失败与降级）按**单源**执行：本技能只以同花顺 iFinD 取数，不作双源复核
> （见「数据源与两条取数路径」）。
> **本技能 = 能力**：数据项抽取、取数入口发现与调用、口径标准化、比对留痕、交付字段产出。

> **KB 逻辑装配表**：本技能 `references/00-KB装配表.md` = 库内层级路径寻址键表（命中本技能 →
> 该表路径并入本次审核下载清单 → 经 crwu-dws 实时下载正文、只读引用、出处 = 本次下载文件:行号 +
> exportedAt；每次审核重新下载、零正文缓存；改知识库内容不动技能）。

## ⚠️ 调用前置条件

本技能是跨方向**能力型**技能：**仅经 `crwu-audit` router 编排调用，禁止单独调用**——默认由总路由
在 `methods[]` 命中 **收益法 / 市场法** 时叠加调用，**禁止 Agent 脱离编排直接调用并据其输出下审核判断**。
例外：用户明确只要「外部数据核验结果」且不做审核判断时，可独立执行——输出仅限核验结果，并注明
"仅外部数据核验，未做审核判断，待方法轴技能/人工判读"。

## 定位与触发

- 何时用：`route_profile.methods[]` 含 `收益法` 或 `市场法`；是否真正取数仍按库内 **表 A** 三条
  （业务线 × 资产线 × 方法线）同时成立判定，缺一即不核验、记"不在本模块范围"。
- 与知识库口径：全部核验口径、数据项取舍与结论档位以库内 `M-外部数据核验` 模块规程为准；
  本技能只提供能力与执行规程，并叠加"单源（同花顺 iFinD）、不启用万得"这一环境事实。
- 与非本技能职责的边界：报告内部与跨表数字勾稽 → `crwu-audit-datacheck`；披露完整性通用检查 →
  `crwu-dev-audit-public-general-standards`；参数合理性与方法判断 → 方法轴技能与人工。

## 数据源与两条取数路径（同花顺 iFinD）

本技能的唯一外部数据源是**同花顺 iFinD**，但宿主不同，取数入口不同——**不要假定另一个宿主的入口存在**：

| 宿主 | 取数路径 | 形态 | 凭据归属 | 探测方式 |
| --- | --- | --- | --- | --- |
| WorkBuddy | 宿主内置连接器 | MCP 服务（连接器 id `ifind-mcp`，服务端授权）；会话可能直接暴露 MCP 工具 | 宿主托管 | 会话工具名 / 宿主连接器声明（`~/.workbuddy`，可用 `CRWU_CONNECTOR_ROOT` 覆盖） |
| DeepSeek Harness | `ifind-finance-data` 技能 | 技能自带 `call.py` / `call-node.js`，直连同花顺 MCP HTTP 服务 | 技能自身的 `mcp_config.json` | 已安装 skills 根下是否存在该技能目录（可用 `CRWU_SKILLS_ROOT` 覆盖） |

- 两条路径取的是**同一上游 iFinD 服务**，但调用方式与凭据来源不同：不要拿 WorkBuddy 的连接器声明
  去推断 DSH 可用，也不要在 DSH 里去找 WorkBuddy 的连接器目录。
- **取数入口发现与调用细则**（发现顺序、降级、留痕契约、两条路径的探测结论）统一见
  `references/01-connector-access.md`；机器可读预检：
  `python3 scripts/connector_probe.py --format json`（只读，不读凭据值）。
- **万得不参与本技能的取数**：库内表 B/C 的源统一为同花顺 iFinD；表 D 的双源复核条件不适用，
  按单源（D1–D4）降级执行。

## 输入

1. router 冻结的 `route_profile`（须含 `methods[]`，以及 `record_context.base_date`——**所有取数的基准日锚点**）；
2. router 已准备并隔离的报告、评估说明、测算明细表等材料路径；
3. router 已验证的本次 DWS manifest，至少含 `references/00-KB装配表.md` 的路径键对应正文。

缺少任一项即记录 capability gap，不自行补取、不沿用上一轮材料。不得接收复核记录。

## 执行流程

0. **基准日锚定（先做）**：从 `route_profile.record_context` 取 `base_date` 与其状态，作为本次全部取数的
   时点锚。**所有区间与窗口由基准日推导**（如"基准日前 2 年"）；禁止"截止最新/最近 N 周"这类
   以取数时点为准的滚动窗口——否则同一项目不同日期复跑结果不同，比对与回归都不成立。
   按基准日状态**分三档**处理（不得整线停载）：

   | 基准日状态 | 取数行为 |
   | --- | --- |
   | 唯一确定 | 正常取数；全部时点参数由该基准日推导 |
   | 多候选（ROUTE003 挂起，存在 2 个及以上候选日） | **条件性取数**：对每个候选日分别取数（或取覆盖全部候选日的区间段）。候选日结论一致 → 可出结论并注明"已在候选基准日下逐一核验，结论一致"；结论不一致 → 出"请说明"的**条件性结论**（注明取决于基准日确认），**不得判"不符合"**，转人工 |
   | 缺失 / 不可解析 | 只核验**不依赖基准日**的数据项（公司属性事实、报告期已固定的财报数、政策文件现行有效性）；依赖时点的项记"未检查"并写明原因 |

   本档只**消费** router 已给出的 `base_date` / `base_date_status`，**不自行判定 ROUTE 冲突**
   （判定权在 `06-overlay-classification.md`）；也**不因基准日挂起而整线停载**——按
   `08-union-dispatch-rules.md`，不依赖冲突字段的规则仍须执行。
1. **抽取**：真实读取报告、评估说明与测算明细表，抽取"报告引用了哪些外部数据"——数据项、报告值、
   报告位置、上下文（用途、方法章节）。**只针对报告实际引用的外部数据**，不凭空扩展核验范围。
2. **定源**：按库内表 B（数据项触及表）与表 C（组合映射表）确定**要核哪些数据项、每项的口径要求**。
   本环境唯一数据源为同花顺 iFinD，**不启用万得**：数据源一律按同花顺 iFinD 取数，
   并如实留痕取数路径与口径变更。
   表内未覆盖的数据项按库内 §9 员工补录机制登记后仍按默认规则核验，不跳过。
3. **取数入口发现**：按 `references/01-connector-access.md` 的顺序探测可用取数路径（WorkBuddy 连接器 /
   DeepSeek Harness 的 `ifind-finance-data` 技能）；只读探测、不读凭据值。
   可用 `python3 scripts/connector_probe.py --format json` 获得机器可读的可用性结论。
4. **取数**：对每个数据项按报告声明的口径（期限、窗口、频率、基准、复权、单位）取数；
   报告**未声明口径**的参数，只做披露检查（输出"请说明"），不取数比对。
5. **口径标准化后比对**：把外部取值与报告值归一到同一口径再比；**口径无法统一时禁止作数值判定**，
   只记录口径差异。事实型数据可判"不符合"；模型型数据只出"请说明"。
6. **输出**：逐项 EXT 意见（结构以库内 §8 意见条目格式为准），并产出交付层
   `externalDataVerification`（供 HTML《外部数据核验》区渲染）。
7. **留痕**：每条核验记录保留库内 §6 要求的全部字段（数据项、数值、来源、取数时点、口径、单位、参数）。

## 交付层字段（供 HTML 体现）

本技能向编排层回执 `externalDataVerification`（写入 AuditResult），结构见
`crwu-audit/references/11-html-delivery-spec.md` 的《外部数据核验》区定义：

- `baseDate`：本次基准日（全部核验的锚点时点）；
- `sources[]`：数据源的可用性事实（`source` / `configured` / `authenticated` / `note`）——
  本技能只申报**同花顺 iFinD** 一条（两条取数路径合并为同一数据源），并在 `note` 里写明本次走的是
  哪条路径；**取数路径不可用时必须在此显式声明**，并给出"相关条目未经外部数据核验"的措辞；
- `checks[]`：逐项核验（数据项、报告值、各源取值与取数时点口径、`deviationAgainst`（与本项目报告值
  出入较大的数据源）、`decision`、报告证据位置、说明）；
- 只要有 `checks` 就**逐项写明正确与不正确**：判定为"符合"的条目同样入表，不得只列问题。

## 禁止事项

1. 禁止用取数时点的滚动窗口代替基准日推导区间；
2. 禁止在口径不一致时判"数据错误"，或仅凭外部取值与报告值的差异否定报告参数；
3. 禁止未留痕取数口径即出数值判定；
4. 禁止把外部数据用作规则依据（外部数据只进报告证据，不进 `rule_source`）；
5. 禁止下方法适用性、参数合理性等专业判断（交方法轴技能与人工）；
6. 禁止在取数路径缺失/未认证时静默跳过：必须按库内降级口径降级并在交付件中显式声明；
7. 禁止声称已做双源复核，或把万得列为本次数据源（本环境不启用万得）。
