# 交易与处置业务规则装配

本表只保存 RULE/CHK 编号和库内层级路径寻址键。运行时由 `crwu-audit` 汇总清单，再由 `crwu-dws` 实时下载正文并生成本次 manifest；目录缓存只用于定位，正文文件零缓存。清单语义与清单外零下载见共同约束 §4。

## 1. 一级根映射（本技能主映射，唯一）

| source_key | owner_axis | canonical_label | kb_root | request_kind | recursive | required |
| --- | --- | --- | --- | --- | --- | --- |
| `BIZ_TRANSACTION_DISPOSAL_ROOT` | `business` | `交易与处置` | `01-业务路线/02-交易与处置/` | `directory` | `true` | `true` |

命中本技能即递归下载该一级根内全部支持正文，不逐文件挑选。`expected_structure` 断言（按本次实时目录核对）：

- 一级根直接文档：`共同审核点` **已提供且为真实内容**（本次 2026-09-10 实时目录 + 逐份正文核对）；它是该一级业务的共用层，命中任一子业务都执行、先于子业务条目，见 `02-review-focus.md`；
- 子业务 `拍卖与处置/`：`01-业务通用审核要点`
- 子业务 `收购与购置/`：`01-业务通用审核要点`
- 子业务 `置换与划转/`：`01-业务通用审核要点`
- 子业务 `转让与出售/`：`01-业务通用审核要点`

库内结构若与上述断言不符，按实际下载结果执行并记知识库内容缺口，不据目录名猜测替代路径、不为子业务另建技能。

## 2. 二级选择索引（在一级根已下载目录包内选用）

| 子业务 | 相对路径 | 要点文件 | 关联索引 |
| --- | --- | --- | --- |
| 拍卖与处置 | `拍卖与处置/` | `01-业务通用审核要点` | — |
| 收购与购置 | `收购与购置/` | `01-业务通用审核要点` | — |
| 置换与划转 | `置换与划转/` | `01-业务通用审核要点` | — |
| 转让与出售 | `转让与出售/` | `01-业务通用审核要点` | — |

关联索引为空文档时按内容缺口处理（见 `02-review-focus.md`）。二级分类结果按 `business_subroutes[]` 返回，只并入本次审核运行记录，不回写 router 已冻结的一级 `route_profile`。

## 3. 其他轴装配

全局执行契约、方法正文、监管正文与表格勾稽由 router 的其他轴（public / method / overlay）装配，不复制进本业务映射；本技能只登记本一级根的路径键。
