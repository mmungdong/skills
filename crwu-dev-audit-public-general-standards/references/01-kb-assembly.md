# 通用准则规则装配

**路径键写法**：逐字使用库内节点名（`crwu-dws` 的 `by_path` 键 = 原样精确名，不做大小写/全半角折叠）。**单文件路径不要加 `.md` 后缀**——`.md` 只是导出到本地时的文件名（`<节点名>.md`），不是库内节点名；带后缀会与 `by_path` 键不匹配而在 M2 记 failure。目录路径以 `/` 结尾并递归下载。

本表只保存 RULE/CHK 编号和库内层级路径寻址键。运行时由 `crwu-audit` 汇总清单，再由 `crwu-dws` 实时下载正文并生成本次 manifest；目录缓存只用于定位，正文文件零缓存，不得读取上一轮下载物。

清单条目有两种语义：不以 `/` 结尾的是单文件路径，只下载该文件；以 `/` 结尾的是目录路径，**递归下载其中全部支持正文**。清单外零下载。

## 1. 装配映射（公共轴：两个根 + 一个单文件 + 一个清单根）

| source_key | owner_axis | canonical_label | kb_root | request_kind | recursive | required |
| --- | --- | --- | --- | --- | --- | --- |
| `PUBLIC_REPORT_DISCLOSURE_ROOT` | `public` | `通用准则` | `06-规则库/02-通用准则-报告与披露/` | `directory` | `true` | `true` |
| `PUBLIC_PROCEDURE_QUALITY_ROOT` | `public` | `通用准则` | `06-规则库/03-通用准则-程序与档案/` | `directory` | `true` | `true` |
| `PUBLIC_BASIS_CURRENCY_CHECK` | `public` | `通用准则` | `06-规则库/01-监管规则与口径/依据时效对照表` | `file` | `false` | `true` |
| `PUBLIC_PITFALL_LIBRARY_ROOT` | `public` | `通用准则` | `06-规则库/易错点库/` | `directory` | `true` | `true` |

四项均为 `required=true`：报告披露层、程序质控层与依据时效判定对任何报告形态恒适用，**易错点库（PIT 条目）为跨对象跨业务的复核反馈沉淀，须对全体项目可见**（此前仅由 `crwu-audit-asset-realestate` 装配 → 其余 11 个资产类型读不到）。
命中本能力即按下表下载，不逐文件挑选、不按对象或方法条件裁剪。

- `PUBLIC_BASIS_CURRENCY_CHECK` 是**单文件**：只下载该文件本身（不带 `.md` 后缀、不以 `/` 结尾）；
  它是 `02-review-focus.md` §2「依据章节的三类可判定缺陷」的判定依据（`RULE-01-03-001~030`），**不是可选参考**。

## 2. expected_structure 断言

**`PUBLIC_REPORT_DISCLOSURE_ROOT`**（2026-09-10 本次实时目录核对）：

- `报告准则-精编条目/`：`README`、`报告准则-总则与基本遵循`、`报告准则-报告内容与附则`、`报告准则-附件格式与声明样稿-参考`；
- `资产评估报告准则`（索引卡：登记元数据与编撰规划，**不构成规则条目、不作引用依据**）。

覆盖 `RULE-01-02-251~279`：`报告准则-总则与基本遵循` = 251~260（第一至十条），`报告准则-报告内容与附则` = 261~279（第十一至二十九条）。

**`PUBLIC_PROCEDURE_QUALITY_ROOT`**：

- `程序准则2018-精编条目/`：`README`、`程序准则旧版2018-总则基本遵循与实施要求上`、`程序准则旧版2018-实施要求下与附则` → `RULE-01-02-101~115`、`RULE-01-02-116~127`；
- `程序准则2026-精编条目/`：`README`、`程序准则2026-第一章总则与第二章基本遵循`、`程序准则2026-第三章实施要求-上`、`程序准则2026-第三章实施要求-下与第四章附则` → `RULE-01-02-001~007`、`RULE-01-02-008~019`、`RULE-01-02-020~028`；
- `质控指南2026-精编条目/`：`README`、`质控指南-总则质量控制责任`、`质控指南-职业道德人力资源承接计划`、`质控指南-实施审核与监控等` → `RULE-01-02-401~417`、`RULE-01-02-418~434`、`RULE-01-02-435~456`；
- 索引卡类（**不构成规则条目、不作引用依据**）：`机构业务质量控制指南2026-6号`、`资产评估程序准则2026版`、`资产评估执业准则-利用专家工作及相关报告-索引卡`。

库内若缺其中某项，按实际下载结果执行并记**知识库内容缺口**，不据目录名猜测替代路径。

**`PUBLIC_BASIS_CURRENCY_CHECK`**（单文件；2026-09-16 本次实时目录核对，`extension=adoc`）：

- `依据时效对照表`：`RULE-01-03-001~003`（三类判定口径：版本过期／分类错位／适用性存疑）+ `RULE-01-03-004~030`（逐条对照）。
- 表内标注 `核验状态=待官方核验` 的行，出意见前须先比对官方原文（见 `02-review-focus.md` §2）。
- 该文件缺失时按 §5 记 gap 并转人工，**不凭经验判定版本新旧**。

**`PUBLIC_PITFALL_LIBRARY_ROOT`**（2026-09-16 本次实时目录核对）：

- `首批易错点`：`PIT-001~030`（错误表现／依据／正确做法·检查动作／历史命中）；
- `README`：易错点条目三要素与引用资格说明。
- 易错点本身不创造规则，须挂 ≥1 条 `RULE-*` A 级条目；未挂依据者只作 B/C 级提示，不作硬依据。

## 3. 版本选择（本轴特有，装配后立即适用）

两个根内**同时存在新旧两版**程序准则，施行期间不同，装配后必须先按报告的基准日／报告日选定版本（判定规则与登记状态见 `02-review-focus.md` §1）。禁止把两版条目混引；禁止用尚未施行的版本倒查在该版本施行前出具的报告。

## 4. 不装配的内容

- 机构内部制度文本：不在本技能范围（机构层面制度对照属部署方与知识库其他目录）。
- 方法与监管覆盖正文：属 method / overlay 轴；其库内目录由 router 按本次 `methods[]`/`overlays[]` 命中项追加（映射唯一事实源见 `crwu-audit/references/08-union-dispatch-rules.md` §「方法层与覆盖层的库内装配映射」），本技能只登记公共准则层与上表四项的寻址键。
- 表格勾稽口径：属 public 轴 `crwu-audit-datacheck`，与工程执行契约一并由其装配。

## 5. 失败与出处

下载零命中、多命中、类型不符、跳过或失败时，以 manifest 状态记录对应 gap，不猜测近似路径。最终出处按 R6 由 manifest 与本次下载物共同形成：本次下载文件、实际行号、库内路径、运行时 `nodeId` 和 `exportedAt`；运行时值不得写回本文件。
